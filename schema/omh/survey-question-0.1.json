{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "description": "This schema represents a survey question.",

    "type": "object",

    "properties": {
        "label": {
            "description": "A label used to identify the question.",
            "type": "string"
        },
        "text": {
            "description": "The text of the question.",
            "type": "string"
        }
    },
    "required": [
        "text"
    ]
}