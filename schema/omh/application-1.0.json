{
    "$schema": "http://json-schema.org/draft-04/schema#",

    "description": "This schema represents an application source of data.",
    "type": "object",

    "definitions": {
    },

    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the source application."
        },
        "id": {
            "type": "string",
            "description": "The id of the source application."
        },
        "provider": {
            "type": "string",
            "description": "The provider of the source application."
        },
        "url": {
            "type": "string",
            "description": "The URL of the source application.",
            "format": "uri"
        },
        "version": {
            "type": "string",
            "description": "The version of the source application."
        },
        "platforms": {
            "type": "array",
            "items": [
                {
                    "os": {
                        "type": "string"
                    },
                    "version": {
                        "type": "string"
                    },
                    "url": {
                        "type": "string",
                        "description": "The URL of the source application.",
                        "format": "uri"
                    }
                }
            ]
        },
        "position_in_lineage": {
            "type": "string"
        }
    },
    "required": [
        "name"
    ]
}
