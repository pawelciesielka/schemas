{
    "$schema": "http://json-schema.org/draft-04/schema#",

    "description": "This schema represents elevation above sea level, either a single body height measurement, or the result of aggregating several measurements made over time (see Numeric descriptor schema for a list of aggregate measures)",
    "type": "object",
    "references": [
        {
            "description": "The SNOMED code represents ??? (observable entity)",
            "url": "http://purl.bioontology.org/ontology/SNOMEDCT/"
        }
    ],
    "definitions": {
        "length_unit_value": {
            "$ref": "../generic/length-unit-value-1.0.json"
        },
        "time_frame": {
            "$ref": "../generic/time-frame-1.0.json"
        },
        "descriptive_statistic": {
            "$ref": "../generic/descriptive-statistic-1.0.json"
        }
    },

    "properties": {
        "elevation": {
            "$ref": "#/definitions/length_unit_value"
        },
        "effective_time_frame": {
            "$ref": "#/definitions/time_frame"
        },
        "descriptive_statistic": {
            "$ref": "#/definitions/descriptive_statistic"
        }
    },

    "required": [ "elevation"]
}